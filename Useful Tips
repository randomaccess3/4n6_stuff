Useful Tips

=========================================================================================================

SQlite3

Dump a blob into a file
sqlite3 database "SELECT writefile('outputFile', heading) from table where row_id = 1"

Dump a blob to hex in the terminal
sqlite3 database "select hex(heading) from table where row_id=1"

=========================================================================================================
